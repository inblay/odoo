<?xml version="1.0"?>
<odoo>

    <record id="update_prm_saletax_wizard_form_view"  model="ir.ui.view">
        <field name="name">update.prm.saletax.form.view</field>
        <field name="model">update.prm.saletax</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="products" invisible="1"/>
                    <field name="taxes_to_add_ids" widget="many2many_tags"/>
                    <field name="taxes_to_exclude_ids" widget="many2many_tags"/>
                </group>
            </form>
        </field>
    </record>

    <record id="update_prm_saletax_action" model="ir.actions.act_window">
         <field name="name">Update customer taxes</field>
         <field name="type">ir.actions.act_window</field>
         <field name="res_model">update.prm.saletax</field>
         <field name="view_type">form</field>
         <field name="view_mode">form</field>
         <field name="view_id" ref="product_management_accounting.update_prm_saletax_wizard_form_view"/>
         <field name="target">new</field>
    </record>


</odoo>
